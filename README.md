## Picture gallery

Objective: to create an interactive web application using modern JavaScript
tools.

## Features

- Modern project structure — modules in the `src` folder.
- Use of the Vite build tool for rapid development and optimization.
- Automatic deployment to GitHub Pages via GitHub Actions.
- Clear file separation:
  - components/HTML-partials in `src/partials`
  - styles in `src/css`
  - images in `src/img`
- Responsive layout (tablet, laptop), modern JS syntax (ES6+), use of module
  imports.

## Technologies

- HTML5
- CSS3
- JavaScript (ES6+)
- Vite
- GitHub Actions (CI/CD)
- GitHub Pages (live version deployment)
